# Paths to exclude when pulling encrypted data from the remote server.
# One path per line. Blank lines and lines starting with # are ignored.
# Leave all lines commented/empty to pull everything (recommended).
